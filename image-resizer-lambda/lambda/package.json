{
  "name": "waitque-image-resizer",
  "version": "1.0.0",
  "description": "Lambda that resizes images uploaded to RAW/ prefix to 100×100 thumbnails and saves them to OPTIMIZED/",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "preinstall": "npm install --cpu=x64 --os=linux sharp"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.1",
    "sharp": "^0.32.6"
  }
}
